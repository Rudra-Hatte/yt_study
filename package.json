{
  "name": "yt-study-app",
  "version": "1.0.0",
  "description": "YouTube Study Platform with AI-powered course generation",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix ai-service\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ai": "npm run dev --prefix ai-service", 
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix ai-service && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "react-hot-toast": "^2.5.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
